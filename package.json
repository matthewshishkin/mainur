{
  "name": "mainur-site",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "serve": "python3 -m http.server 3333 --bind 127.0.0.1",
    "serve:lan": "python3 -m http.server 3333 --bind 0.0.0.0"
  }
}
